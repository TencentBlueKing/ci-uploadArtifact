input.filePath.label=Documents to be archived
input.filePath.placeholder=The list of files that need to be archived supports Glob wildcards (the directory name can only contain * or ** wildcards, and the file name to be matched supports all Glob wildcards),\r\ndoes not support archive directories
input.filePath.desc=Archive the build products under the relative path or absolute path directory of the current workspace. Wildcards can be used to match, and multiple paths (separated by , in English) are supported.\r\ndoes not support archive directories
input.repoName.list[0].label=Pipeline warehouse
input.repoName.list[1].label=Custom warehouse
input.isParentPipeline.text=Archive to parent pipeline
input.downloadFiles.label=The file that needs to output the download link
input.downloadFiles.parameters[0].rowAttributes[0].label=File Path
input.downloadFiles.parameters[0].rowAttributes[0].placeholder=File Path
input.downloadFiles.parameters[0].rowAttributes[0].desc=The relative or absolute path of a single file in the current workspace, wildcards are not supported
input.downloadFiles.parameters[0].rowAttributes[1].label=Output Variable Name
input.downloadFiles.parameters[0].rowAttributes[1].placeholder=Output Variable Name
input.downloadFiles.parameters[0].rowAttributes[1].desc=Receive the variable name corresponding to the download link
input.downloadFiles.desc=Specify the file that needs to generate the download link, and receive the variable of the corresponding download link, so that the link can be obtained through the variable in the downstream plug-in
input.metadata.label=Custom Metadata
input.metadata.parameters[0].rowAttributes[0].label=Metadata Attribute Key
input.metadata.parameters[0].rowAttributes[0].placeholder=Metadata Attribute Key
input.metadata.parameters[0].rowAttributes[0].desc=Metadata Attribute Key
input.metadata.parameters[0].rowAttributes[1].label=Metadata Attribute Value
input.metadata.parameters[0].rowAttributes[1].placeholder=Metadata Attribute Value
input.metadata.parameters[0].rowAttributes[1].desc=Metadata Attribute Value
input.metadata.desc=Add custom metadata to all artifacts in the archive
input.enableMD5Checksum.text=Enable MD5 check
input.enableMD5Checksum.desc=Check whether the MD5 of the local file and the file uploaded to the product library are consistent

releaseInfo.name=UploadArtifact
releaseInfo.summary=This plugin archives the local files of the builder to the pipeline warehouse or custom warehouse, and archives the output.
